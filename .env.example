# Mock mode is the default and requires no Shopify credentials.
SHOPIFY_MODE=mock

# Required only after intentionally switching SHOPIFY_MODE to live.
SHOPIFY_SHOP=
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=

# Optional non-secret override. Defaults to 2026-07 when omitted.
SHOPIFY_API_VERSION=
